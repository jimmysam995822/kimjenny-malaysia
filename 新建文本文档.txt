import React, { useState, useEffect, useRef, useMemo } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { ShoppingBag, ChevronRight, Wind, ShieldCheck, Sparkles, ArrowRight, BookOpen, Layers, X, CheckCircle } from 'lucide-react';

/** * --- 设计令牌 (高奢级视觉体系) ---
 * 字体: Playfair Display (顶级衬线) & Inter (极简无衬线)
 * 颜色: 雅致白 (#F9F8F6), 深墨色 (#1A1A1A), 柔金色 (#D4AF37)
 * 交互: A/B面与尺寸快速联动切换 + 真实视频背景注入 + 极致沙箱防黑屏容错
 */

// --- 动画组件: 文字渐显特效 ---
const buildKeyframes = (from, steps) => {
  const keys = new Set([...Object.keys(from), ...steps.flatMap(s => Object.keys(s))]);
  const keyframes = {};
  keys.forEach(k => {
    keyframes[k] = [from[k], ...steps.map(s => s[k])];
  });
  return keyframes;
};

const BlurText = ({
  text = '',
  delay = 150,
  className = '',
  animateBy = 'words',
  direction = 'top',
  threshold = 0.1,
  rootMargin = '0px',
  animationFrom,
  animationTo,
  easing = [0.25, 0.46, 0.45, 0.94],
  onAnimationComplete,
  stepDuration = 0.5
}) => {
  const elements = animateBy === 'words' ? text.split(' ') : text.split('');
  const [inView, setInView] = useState(false);
  const ref = useRef(null);

  useEffect(() => {
    if (!ref.current) return;
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setInView(true);
          observer.unobserve(ref.current);
        }
      },
      { threshold, rootMargin }
    );
    observer.observe(ref.current);
    return () => observer.disconnect();
  }, [threshold, rootMargin]);

  const defaultFrom = useMemo(
    () => direction === 'top' 
      ? { filter: 'blur(15px)', opacity: 0, y: -40 } 
      : { filter: 'blur(15px)', opacity: 0, y: 40 },
    [direction]
  );

  const defaultTo = useMemo(
    () => [
      { filter: 'blur(5px)', opacity: 0.5, y: direction === 'top' ? 5 : -5 },
      { filter: 'blur(0px)', opacity: 1, y: 0 }
    ],
    [direction]
  );

  const fromSnapshot = animationFrom ?? defaultFrom;
  const toSnapshots = animationTo ?? defaultTo;

  const stepCount = toSnapshots.length + 1;
  const totalDuration = stepDuration * (stepCount - 1);
  const times = Array.from({ length: stepCount }, (_, i) => (stepCount === 1 ? 0 : i / (stepCount - 1)));

  return (
    <div ref={ref} className={`${className} flex flex-wrap`}>
      {elements.map((segment, index) => {
        const animateKeyframes = buildKeyframes(fromSnapshot, toSnapshots);
        return (
          <motion.span
            key={index}
            className="inline-block will-change-[transform,filter,opacity]"
            initial={fromSnapshot}
            animate={inView ? animateKeyframes : fromSnapshot}
            transition={{
              duration: totalDuration,
              times,
              delay: (index * delay) / 1000,
              ease: easing
            }}
          >
            {segment === '' ? '\u00A0' : segment}
            {animateBy === 'words' && index < elements.length - 1 && '\u00A0'}
          </motion.span>
        );
      })}
    </div>
  );
};

const GlobalStyles = () => (
  <style>{`
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:ital,wght=0,400;0,700;1,400&display=swap');
    
    :root {
      --alabaster: #F9F8F6;
      --charcoal: #1A1A1A;
      --gold: #D4AF37;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--alabaster);
      color: var(--charcoal);
      -webkit-font-smoothing: antialiased;
      overflow-x: hidden;
      scroll-behavior: smooth;
    }

    .font-serif { font-family: 'Playfair Display', serif; }

    .liquid-glass {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      box-shadow: inset 0 1px 1px rgba(255, 255, 255, 0.05);
    }

    .noise-overlay {
      position: fixed;
      inset: 0;
      z-index: 9999;
      pointer-events: none;
      opacity: 0.02;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
    }

    .vertical-label {
      writing-mode: vertical-rl;
      text-orientation: mixed;
    }
  `}</style>
);

const App = () => {
  const [isScrolled, setIsScrolled] = useState(false);
  const [toastMessage, setToastMessage] = useState(null);

  // 规格联动状态机制 (150x200, 180x220, 200x230)
  const [sizeP1, setSizeP1] = useState('200x230cm');
  const [sizeP2, setSizeP2] = useState('200x230cm');

  // 用户自定义上传的图片状态
  const [customImgP1, setCustomImgP1] = useState(null);
  const [customImgP2, setCustomImgP2] = useState(null);

  // 图像容错安全机制 
  const [imgErrP1, setImgErrP1] = useState(false);
  const [imgErrP2, setImgErrP2] = useState(false);
  const [imgErrStory, setImgErrStory] = useState(false);

  // --- 商品 1：Classic 真实规格与大马定价数据库 ---
  const dataP1 = {
    '150x200cm': { price: '132.93', origin: '443.09', label: 'Single Size 适合单人' },
    '180x220cm': { price: '156.38', origin: '521.25', label: 'Queen Size 标准双人' },
    '200x230cm': { price: '172.28', origin: '574.24', label: 'King Size 豪华加大' }
  };

  // --- 商品 2：Luxury 真实规格与大马定价数据库 ---
  const dataP2 = {
    '150x200cm': { price: '209.91', origin: '699.67', label: 'Single Size 适合单人' },
    '180x220cm': { price: '236.79', origin: '789.27', label: 'Queen Size 标准双人' },
    '200x230cm': { price: '259.54', origin: '865.13', label: 'King Size 豪华加大' }
  };

  useEffect(() => {
    const handleScroll = () => setIsScrolled(window.scrollY > 50);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  const triggerToast = (msg) => {
    setToastMessage(msg);
    setTimeout(() => setToastMessage(null), 4000);
  };

  const handleShopRedirect = (productTitle) => {
    const titleText = productTitle || 'KimJenny 官方旗舰店';
    triggerToast(`正在为您安全连接至 Shopee: ${titleText}`);
    
    // 邓总的真实大马 Shopee 店铺链接
    const shopeeUrl = "https://shopee.com.my/kimjenny.my?entryPoint=ShopBySearch&searchKeyword=kimjenny%20bedding%20%26living";
    
    setTimeout(() => {
      window.open(shopeeUrl, '_blank');
    }, 800);
  };

  return (
    <div className="relative">
      <GlobalStyles />
      {/* 奢侈噪点覆盖层 */}
      <div className="noise-overlay" />

      {/* --- 背景优雅辅助线 (对称杂志风) --- */}
      <div className="fixed inset-0 z-0 pointer-events-none flex justify-between px-8 md:px-16 lg:px-24">
        {[...Array(4)].map((_, i) => (
          <div key={i} className="w-px h-full bg-[#1A1A1A]/[0.03]" />
        ))}
      </div>

      {/* --- 顶部配送专线通告 --- */}
      <div className="bg-[#1A1A1A] text-white text-[10px] tracking-[0.3em] py-2 text-center uppercase relative z-[101]">
        ✨ Free Shipping Across Malaysia for Orders Over RM 150 ✨
      </div>

      {/* --- 极简高奢导航 --- */}
      <nav className="fixed w-full z-[100] px-6 md:px-12 lg:px-16 pt-12 transition-all duration-700">
        <div className={`liquid-glass flex items-center justify-between px-6 py-3 rounded-none border border-[#1A1A1A]/10 transition-all ${isScrolled ? 'bg-white/90 shadow-sm border-b border-gray-200' : ''}`}>
          <div className="text-2xl font-serif font-bold tracking-tighter uppercase cursor-pointer" onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}>
            KimJenny<span className="text-[#D4AF37]">.</span>
          </div>
          
          <div className="hidden md:flex gap-12 text-[11px] uppercase tracking-[0.3em] font-medium">
            <a href="#home" className="hover:text-[#D4AF37] transition-all duration-300 relative group py-1">
              首页 Home
            </a>
            <a href="#products" className="hover:text-[#D4AF37] transition-all duration-300 relative group py-1">
              产品介绍 Products
            </a>
            <a href="#story" className="hover:text-[#D4AF37] transition-all duration-300 relative group py-1">
              品牌故事 Story
            </a>
          </div>

          <div className="flex items-center gap-4">
            <div className="hidden lg:flex items-center gap-2 border border-black/10 px-3 py-1 text-[10px] uppercase tracking-widest font-semibold">
              <span className="w-1.5 h-1.5 bg-[#D4AF37] rounded-full animate-pulse"></span>
              Malaysia MYR (RM)
            </div>
            <button 
              onClick={() => handleShopRedirect('KimJenny Summer Quilts')}
              className="group relative overflow-hidden bg-[#1A1A1A] text-white px-6 md:px-8 py-3 text-[10px] uppercase tracking-[0.2em] transition-all"
            >
              <span className="relative z-10">立即选购 Shop Now</span>
              <div className="absolute inset-0 bg-[#D4AF37] translate-x-[-101%] group-hover:translate-x-0 transition-transform duration-500 ease-out" />
            </button>
          </div>
        </div>
      </nav>

      {/* --- 第一页：首屏品牌秀 (Hero Section - 🎬 真实视频背景 + 完美无闪图层合体) --- */}
      <section 
        id="home" 
        className="relative h-screen w-full overflow-hidden bg-[#000] flex items-center"
        style={{
          backgroundImage: "url('7c0702fa0d8133373671afb5ace05bfc.jpg')",
          backgroundSize: 'cover',
          backgroundPosition: 'center',
          backgroundRepeat: 'no-repeat'
        }}
      >
        <div className="absolute inset-0 z-0">
          {/* 🎯 核心防黑屏方案：双面海报 poster 缓冲机制，确保加载流畅无缝 */}
          <video 
            autoPlay 
            loop 
            muted 
            playsInline
            poster="7c0702fa0d8133373671afb5ace05bfc.jpg"
            className="w-full h-full object-cover opacity-70 scale-100 transition-all duration-1000"
            style={{ minWidth: '100%', minHeight: '100%' }}
          >
            <source src="341a41fe01ffaf6df15c101758656283_raw.mp4" type="video/mp4" />
            您的浏览器不支持视频背景。
          </video>
          <div className="absolute inset-0 bg-gradient-to-t from-black via-black/40 to-transparent"></div>
        </div>

        <div className="relative z-20 w-full px-8 md:px-16 lg:px-24 text-white">
          <div className="lg:grid lg:grid-cols-2 lg:items-end">
            <div className="max-w-4xl">
              <div className="flex items-center gap-4 mb-6">
                <div className="h-px w-12 bg-[#D4AF37]" />
                <span className="text-[10px] uppercase tracking-[0.4em] text-[#D4AF37]">2026 夏季极简睡眠美学 The Summer Aesthetic</span>
              </div>
              
              {/* 完美对齐的邓总专属大 H1 标语 */}
              <BlurText 
                text="夏季必备，颜色多种可选，定制您的私人卧室。" 
                className="text-4xl md:text-6xl lg:text-7xl font-serif leading-[1.1] mb-6 tracking-tight font-bold"
                animateBy="letters"
                direction="bottom"
                delay={40}
              />

              <BlurText 
                text="源头工厂直供。A类母婴安全标准，专为马来西亚炎热潮湿的夏天重塑物理恒温裸睡。大马专属对公、发货全面对接。" 
                className="text-sm md:text-base font-light text-white/70 max-w-xl mb-10 leading-relaxed"
                animateBy="words"
                direction="bottom"
                delay={80}
              />

              <div className="flex flex-wrap gap-6">
                <a href="#products" className="group relative overflow-hidden bg-white text-black px-10 py-5 text-xs uppercase tracking-[0.25em] font-semibold transition-all">
                  <span className="relative z-10">浏览真实商品系列 Products</span>
                  <div className="absolute inset-0 bg-[#D4AF37] translate-x-[-101%] group-hover:translate-x-0 transition-transform duration-500 ease-out" />
                </a>
              </div>
            </div>

            <div className="hidden lg:flex justify-end">
              <div className="liquid-glass border border-white/10 px-8 py-5 text-right backdrop-blur-md">
                <p className="text-[10px] uppercase tracking-[0.3em] text-white/40 mb-2">马来西亚全境极速直达</p>
                <p className="text-xl font-serif italic text-[#D4AF37]">Nantong Factory to West/East Malaysia</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* --- 第二页：产品介绍区（Products - 保持真实 Shopee 数据不变） --- */}
      <section id="products" className="py-32 bg-[#F9F8F6]">
        <div className="max-w-[1600px] mx-auto px-8 md:px-16 lg:px-24">
          <div className="flex flex-col items-center justify-center text-center mb-24">
            <h2 className="text-4xl md:text-6xl font-serif leading-tight text-[#1A1A1A] mb-4">
              夏季专属 <span className="italic">*真实爆款系列*</span> Products
            </h2>
            <p className="text-xs uppercase tracking-[0.4em] text-gray-400">
              中国家纺名城南通源头工坊 1:1 精品直营 · 已在大马虾皮全面上架
            </p>
          </div>

          <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 lg:gap-32">
            
            {/* --- 商品 1：Classic Ice Silk --- */}
            <div className="bg-white p-8 border border-black/5 hover:shadow-[0_20px_50px_rgba(0,0,0,0.06)] transition-all duration-700 flex flex-col justify-between">
              <div>
                <div className="relative aspect-square overflow-hidden mb-8 shadow-sm group">
                  <img 
                    src={customImgP1 || (imgErrP1 ? "https://images.unsplash.com/photo-1505693416388-ac5ce068fe85?q=80&w=1200" : "7c0702fa0d8133373671afb5ace05bfc.jpg")} 
                    alt="KimJenny Classic Ice Silk Cooling Quilt" 
                    onError={() => setImgErrP1(true)}
                    className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-1000"
                  />
                  <div className="absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center z-20">
                    <label className="cursor-pointer bg-white text-[#1A1A1A] px-6 py-3 text-[10px] uppercase tracking-widest font-bold hover:bg-[#D4AF37] hover:text-white transition-all shadow-lg">
                      点击上传新图片 Upload
                      <input type="file" accept="image/*" className="hidden" onChange={(e) => {
                        if (e.target.files && e.target.files[0]) {
                          setCustomImgP1(URL.createObjectURL(e.target.files[0]));
                        }
                      }} />
                    </label>
                  </div>
                  <div className="absolute top-6 left-6 bg-[#1A1A1A] text-white px-4 py-2 text-[10px] uppercase tracking-widest font-bold pointer-events-none z-10">
                    大马经典爆款 · Classic Ice Silk
                  </div>
                </div>

                <h3 className="text-xl md:text-2xl font-serif font-bold text-gray-900 leading-snug mb-3">
                  KimJenny Classic Ice Silk Cooling Quilt | Lightweight Air Cond Blanket | Essential Soft Bedding
                </h3>
                <p className="text-xs text-gray-400 uppercase tracking-widest mb-6">A类母婴级安全 · B面极速凉感锦氨面料</p>
                <p className="text-sm text-gray-500 mb-8 leading-relaxed">
                  大马炎热多雨季节的绝佳床品。采用物理凉感冰丝面料，整夜开着空调盖上它，能给身体一个22°C微风般的清凉拥抱，触肤即降温。
                </p>

                <div className="mb-8">
                  <span className="block text-xs font-bold text-gray-400 uppercase tracking-widest mb-3 flex items-center gap-1">
                    <Layers size={14} className="text-[#D4AF37]" /> 选择大马规格尺寸 (Select Size):
                  </span>
                  <div className="flex gap-3">
                    {Object.keys(dataP1).map((sz) => (
                      <button
                        key={sz}
                        onClick={() => setSizeP1(sz)}
                        className={`px-4 py-2 text-xs border transition-all ${sizeP1 === sz ? 'bg-black text-white border-black' : 'bg-transparent text-gray-500 border-gray-200 hover:border-black'}`}
                      >
                        {sz}
                      </button>
                    ))}
                  </div>
                  <span className="block text-[11px] text-[#D4AF37] mt-2 font-medium">*{dataP1[sizeP1].label}</span>
                </div>
              </div>

              <div className="border-t border-gray-100 pt-6 flex justify-between items-center">
                <div>
                  <div className="text-3xl font-serif font-bold text-[#EE4D2D]">
                    RM {dataP1[sizeP1].price}
                  </div>
                  <span className="text-xs text-gray-400 line-through">
                    RM {dataP1[sizeP1].origin}
                  </span>
                </div>
                <button 
                  onClick={() => handleShopRedirect(`Classic Ice Silk (${sizeP1})`)}
                  className="bg-black text-white px-6 py-3 text-[10px] uppercase tracking-widest font-bold hover:bg-[#D4AF37] transition-all"
                >
                  前往 Shopee 购买
                </button>
              </div>
            </div>

            {/* --- 商品 2：Luxury Soy Fiber --- */}
            <div className="bg-white p-8 border border-black/5 hover:shadow-[0_20px_50px_rgba(0,0,0,0.06)] transition-all duration-700 flex flex-col justify-between">
              <div>
                <div className="relative aspect-square overflow-hidden mb-8 shadow-sm group">
                  <img 
                    src={customImgP2 || (imgErrP2 ? "https://images.unsplash.com/photo-1505693395321-883724634266?q=80&w=1200" : "e3f2e45fb4dd0ca4346dcd1766c339a1.jpg")} 
                    alt="KimJenny Luxury Soy Fiber Summer Quilt" 
                    onError={() => setImgErrP2(true)}
                    className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-1000"
                  />
                  <div className="absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center z-20">
                    <label className="cursor-pointer bg-white text-[#1A1A1A] px-6 py-3 text-[10px] uppercase tracking-widest font-bold hover:bg-[#D4AF37] hover:text-white transition-all shadow-lg">
                      点击上传新图片 Upload
                      <input type="file" accept="image/*" className="hidden" onChange={(e) => {
                        if (e.target.files && e.target.files[0]) {
                          setCustomImgP2(URL.createObjectURL(e.target.files[0]));
                        }
                      }} />
                    </label>
                  </div>
                  <div className="absolute top-6 left-6 bg-[#D4AF37] text-white px-4 py-2 text-[10px] uppercase tracking-widest font-bold pointer-events-none z-10">
                    大马高奢奢华款 · Luxury Soy Fiber
                  </div>
                </div>

                <h3 className="text-xl md:text-2xl font-serif font-bold text-gray-900 leading-snug mb-3">
                  KimJenny Luxury Soy Fiber Summer Quilt | French Jacquard with Tassels | Premium Thick Comforter
                </h3>
                <p className="text-xs text-gray-400 uppercase tracking-widest mb-6">大豆纤维奢华填充 · 法式高奢编织工艺</p>
                <p className="text-sm text-gray-500 mb-8 leading-relaxed">
                  追求极致法式优雅和软糯触感的奢华之选。采用优质天然大豆纤维填充，手感饱满轻柔、透气干爽，B面带有法式手工吊穗，卧室美学格调瞬间拉满。
                </p>

                <div className="mb-8">
                  <span className="block text-xs font-bold text-gray-400 uppercase tracking-widest mb-3 flex items-center gap-1">
                    <Layers size={14} className="text-[#D4AF37]" /> 选择大马规格尺寸 (Select Size):
                  </span>
                  <div className="flex gap-3">
                    {Object.keys(dataP2).map((sz) => (
                      <button
                        key={sz}
                        onClick={() => setSizeP2(sz)}
                        className={`px-4 py-2 text-xs border transition-all ${sizeP2 === sz ? 'bg-black text-white border-black' : 'bg-transparent text-gray-500 border-gray-200 hover:border-black'}`}
                      >
                        {sz}
                      </button>
                    ))}
                  </div>
                  <span className="block text-[11px] text-[#D4AF37] mt-2 font-medium">*{dataP2[sizeP2].label}</span>
                </div>
              </div>

              <div className="border-t border-gray-100 pt-6 flex justify-between items-center">
                <div>
                  <div className="text-3xl font-serif font-bold text-[#EE4D2D]">
                    RM {dataP2[sizeP2].price}
                  </div>
                  <span className="text-xs text-gray-400 line-through">
                    RM {dataP2[sizeP2].origin}
                  </span>
                </div>
                <button 
                  onClick={() => handleShopRedirect(`Luxury Soy Fiber (${sizeP2})`)}
                  className="bg-black text-white px-6 py-3 text-[10px] uppercase tracking-widest font-bold hover:bg-[#D4AF37] transition-all"
                >
                  前往 Shopee 购买
                </button>
              </div>
            </div>

          </div>
        </div>
      </section>

      {/* --- 第三页：品牌故事区 (Story Section) --- */}
      <section id="story" className="py-32 bg-white relative">
        <div className="max-w-[1600px] mx-auto px-8 md:px-16 lg:px-24">
          <div className="grid grid-cols-1 lg:grid-cols-12 gap-20 items-center">
            
            <div className="lg:col-span-5 relative">
              <div className="vertical-label absolute -left-12 top-0 text-[10px] uppercase tracking-[0.4em] text-gray-300 hidden md:block">
                KIMJENNY AESTHETIC PHILOSOPHY / 2026
              </div>
              <div className="group relative aspect-[3/4] overflow-hidden bg-gray-100 shadow-[0_8px_32px_rgba(0,0,0,0.05)]">
                <img 
                  src={imgErrStory ? "https://images.unsplash.com/photo-1616627547584-bf28cee262db?q=80&w=2560" : "7c0702fa0d8133373671afb5ace05bfc.jpg"} 
                  alt="KimJenny Story Visual" 
                  onError={() => setImgErrStory(true)}
                  className="w-full h-full object-cover grayscale hover:grayscale-0 transition-all duration-[2000ms] ease-out"
                />
              </div>
            </div>

            <div className="lg:col-span-6 lg:col-start-7">
              <h2 className="text-[10px] uppercase tracking-[0.4em] text-[#D4AF37] mb-6 flex items-center gap-2">
                <BookOpen size={14} /> SUMMER ESSENTIALS / 夏日必备系列
              </h2>
              <h3 className="text-4xl md:text-6xl font-serif leading-tight mb-8 text-[#1A1A1A]">
                大马夏日 <span className="italic">*必备单品*</span>，<br className="hidden lg:block" />
                随心搭配您的专属 <span className="italic">*卧室*</span>。
              </h3>
              
              <div className="text-base text-gray-500 leading-relaxed max-w-xl space-y-6">
                <p>
                  <span className="float-left text-7xl font-serif mr-4 leading-[0.8] text-[#1A1A1A]">K</span>
                  imJenny 深知热带夏夜的痛点。在炎炎夏日，一床真正懂您的凉被是每个大马家庭的绝对必备。我们的夏凉被采用高阶冰丝与透气锦氨面料，触肤即降温，仿佛拥抱了一阵 22°C 的清凉微风，让身体瞬间沉浸在无压力的深度睡眠中。
                </p>
                <p>
                  为了让清凉不仅停留在触感，我们为您准备了极其丰富的色彩选择。数十种高颜值的治愈色系与法式印花，让您能根据每天的心情与家居风格，<strong className="text-[#1A1A1A] font-semibold">自由搭配您的卧室空间</strong>。无论是极简的纯白、治愈的奶咖，还是温柔的薰衣草紫，总有一款能完美点亮您的私密天地。
                </p>
                <p className="border-l-2 border-[#D4AF37] pl-4 italic text-[#1A1A1A]/80 font-serif">
                  "一床夏日必备的完美凉被，不仅要懂得为您驱散炎热，更要能随心装扮您的卧室，治愈一整天的心情。"
                </p>
              </div>

              <div className="border-t border-gray-100 mt-12 pt-8 flex items-center justify-between">
                <div>
                  <p className="text-sm font-serif font-bold text-gray-800">KimJenny Aesthetics</p>
                  <p className="text-[10px] text-gray-400 uppercase tracking-widest">Summer Essentials</p>
                </div>
                <div className="h-px w-24 bg-[#D4AF37]/40" />
              </div>
            </div>

          </div>
        </div>
      </section>

      {/* --- 页脚区域 --- */}
      <footer className="bg-[#1A1A1A] text-white py-32 px-8 md:px-16 lg:px-24">
        <div className="max-w-[1600px] mx-auto">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-24 items-center mb-24">
            <h2 className="text-4xl md:text-6xl font-serif leading-tight">
              准备好 <span className="italic text-[#D4AF37]">重塑</span> 你的夜晚了吗？
            </h2>
            <div className="flex flex-col gap-8">
              <p className="text-sm text-white/50 leading-relaxed max-w-sm">
                和马来西亚 4000+ 位深度睡眠大热店铺一起，用最体面、高奢且理性的出厂价，享受奢华极致好梦。
              </p>
              <div className="flex gap-4">
                <button 
                  onClick={() => handleShopRedirect('KimJenny Footer Link')}
                  className="bg-[#D4AF37] text-white px-8 py-4 text-xs uppercase tracking-[0.25em] font-semibold hover:bg-white hover:text-black transition-all duration-500"
                >
                  前往 官方旗舰店
                </button>
              </div>
            </div>
          </div>
          
          <div className="pt-24 border-t border-white/10 flex flex-col items-center justify-center gap-6">
            <div className="text-2xl font-serif font-bold uppercase tracking-tighter">KimJenny<span className="text-[#D4AF37]">.</span></div>
            <p className="text-[10px] uppercase tracking-[0.2em] text-white/20 text-center">
              © 2026 南通弘曜跨境电子商务有限公司 & KimJenny Aesthetics. 版权所有。
            </p>
          </div>
        </div>
      </footer>

      {/* --- 定制黑金 Toast 提示框 --- */}
      <AnimatePresence>
        {toastMessage && (
          <motion.div
            initial={{ opacity: 0, y: 50, x: '50%' }}
            animate={{ opacity: 1, y: 0, x: '0%' }}
            exit={{ opacity: 0, y: 20, transition: { duration: 0.3 } }}
            className="fixed bottom-6 right-6 z-[200] bg-white text-[#1A1A1A] px-6 py-4 shadow-[0_20px_50px_rgba(0,0,0,0.15)] border-l-4 border-[#D4AF37] flex items-center gap-4 max-w-md"
          >
            <CheckCircle className="text-[#D4AF37] flex-shrink-0" size={20} />
            <div className="flex-1">
              <p className="text-xs font-bold uppercase tracking-wider text-gray-400">系统跳转提示</p>
              <p className="text-sm font-medium mt-0.5">{toastMessage}</p>
            </div>
            <button onClick={() => setToastMessage(null)} className="text-gray-400 hover:text-black transition-colors">
              <X size={16} />
            </button>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
};

export default App;